
# Frontend Dockerfile (example: React / Nginx)
FROM node:18-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine

COPY index.html /usr/share/nginx/html/index.html

EXPOSE 80